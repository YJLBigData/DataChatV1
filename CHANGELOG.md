# 更新日志

本项目的所有显著变更都将记录在此文件中。

格式基于 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，
本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。更新日志面向使用者总结能力变化，不是 Git 提交记录的逐条复制。

> 当前仓库尚未创建 Git 版本标签或正式 Release。以下内容根据 `b95a8336` 至 `c622885a`（2026-05-20 至 2026-06-25）的提交历史整理，首次正式发版前均归入 `[Unreleased]`。

## [Unreleased]

### 新增 / Added

- **问数准确率闭环** — 在 SQL 编译前增加 `AccuracyCritic` 结构化复核和至多一次确定性修复，在答案生成前增加 `ResultValidator`，检查缺列、TopN 行数、排序、空结果和指标全空，并补充覆盖核心问数场景的准确率基准集。
- **激活一线人员语义资产** — 语义层新增激活一线人员月度指标汇总表，覆盖销售代表、动销、终端销售与达成、过单与达成、鹤礼 3.0 新客和复购等指标。
- **门店明细分析维度** — 门店级明细语义补充地区、连锁系统、渠道等级、产品品类、月日均和前三月日均等口径，并明确同名渠道、圈层与品类字段的差异。
- **专家团协同分析** — 新增决策调度总监与销售、渠道、用户运营、市场、财务、数据审计等专家，支持同步/异步任务、会话、收藏夹、自定义专家技能和报告合成。
- **智能小Q集成** — 接入 Quick BI 智能小Q数据集列表、启用状态和问数接口，支持多数据集原生查询及逐数据集降级，并可向专家团传递所选数据集。
- **可信结果导出** — 新增按会话和 `trace_id` 创建的异步 Excel 导出队列，提供状态查询、下载、取消、过期清理和队列背压。
- **多模型运行时切换** — 支持百炼与飞鹤统一模型网关按请求切换，管理端可维护多套 LLM 预设；预设通过连通性测试后才能保存或设为默认。
- **可观测与容量治理** — 增加阶段耗时、LLM 等待、HTTP 与问数并发指标，以及显式线程池、全局在途并发闸、数据库连接池参数、超时控制、Redis 容量策略、Nginx/systemd 配置和 k6 压测脚本。
- **管理与协作能力** — 提供用户、权限、语义层版本、LLM 配置、日志、报告模板、会话收藏、DOCX 报告和飞书卡片推送等管理接口与前端页面。

### 变更 / Changed

- **仓库边界收敛** — 移除曾短期集成的 Java DataCode 子服务，当前仓库只保留 DataChat 主系统；DataCode 作为独立服务维护。
- **问数链路模块化** — 后端按会话、收藏夹、报告、飞书、管理、LLM、专家团、智能小Q和导出等领域拆分 `APIRouter`，前端同步拆分页面、API 与状态 hooks。
- **生产执行口径** — 最终部署基线统一为单 Uvicorn worker、显式线程池和并发闸；开启多 worker 前必须先解决任务状态和写热 SQLite 的跨进程一致性。
- **语义层生产适配** — 支持按表通过环境变量覆盖 schema，生产语义资产对齐 `chatbi` / `hs_poc`，并随仓库提供预构建检索索引，降低生产启动对 embedding 服务的依赖。
- **真实数据月份解析** — 相对时间查询优先探测目标表真实最新月份并短时缓存；探测失败才回退语义层配置，避免“本月”静默查询到尚无数据的月份。
- **缓存一致性** — L2 答案缓存改为以规范化 `QueryPlan` 签名为键，并维护“问题到计划签名”的索引，减少跨会话复用错误。

### 修复 / Fixed

- **误澄清与指标遗漏** — 结构完整且用户已明确指标时不再被模型错误标记为必须澄清；多指标、TopN、占比、差异、同比环比和多轮下钻场景可在编译前补齐或转为安全澄清。
- **答案与飞书调用** — 移除 JSON 修复造成的重复 LLM 调用；管理员推送必须显式指定目标，普通用户只能推送给本人绑定邮箱。
- **模型网关兼容** — 兼容飞鹤网关不同环境变量命名、模型探测和超时配置，网关不可用时允许结构化规划链路安全降级。
- **旧版 SQLite 兼容** — LLM 设置持久化兼容 CentOS 7 自带的 SQLite 3.7。
- **本地启动与发布** — 修复依赖安装卡住、前端构建产物遗漏和专家团 `cube_ids` 传递问题；发布脚本在推送前执行前端类型检查、构建和后端测试。

### 安全 / Security

- **只读 SQL 防护** — 所有问数 SQL 必须通过 `sqlglot` AST 校验、危险语句拦截、表白名单、自动 `LIMIT`、列权限校验和行级过滤，只允许只读查询。
- **资源归属校验** — 会话、收藏夹、导出任务和下载文件均按用户校验归属；导出内容只读取服务端可信问数结果，不接受客户端 SQL。
- **外部调用与错误脱敏** — 飞书接口禁止客户端提交任意 webhook 地址，智能小Q诊断对密钥脱敏，面向用户的异常统一返回安全提示和 `trace_id`。
